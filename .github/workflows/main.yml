name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Unzip
        run: unzip AppAppel2-4.zip
      - name: Build
        run: cd AppAppel2 && chmod +x gradlew && ./gradlew assembleDebug
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: AppAppel2-debug
          path: AppAppel2/app/build/outputs/apk/debug/app-debug.apk
